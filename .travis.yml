language: swift
os: osx
osx_image: xcode10.2
xcode_project: it-events-by.xcodeproj
xcode_scheme: it-events-by-Develop
xcode_destination: platform=iOS Simulator,OS=12.2,name=iPhone X

before_install:
 - gem install cocoapods
 - pod repo update
 - pod install
